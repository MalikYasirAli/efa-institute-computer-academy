import React from 'react'

const Home: React.FC = () => (
  <section>
    <h1 className="text-2xl font-bold">EFA Institute of Computer Academy</h1>
    <p className="mt-4 text-gray-600">Welcome — this is the React/TypeScript foundation. Content will be migrated from the existing static site.</p>
  </section>
)

export default Home
